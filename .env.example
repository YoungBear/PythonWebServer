# 服务协议: http / https（默认 https）
SERVER_PROTOCOL=https
# 是否校验客户端证书（默认 true，仅 HTTPS 生效）
VERIFY_CLIENT_CERT=true
# 监听地址 / 端口（默认 0.0.0.0 / 8888）
SERVER_HOST=0.0.0.0
SERVER_PORT=8888
# 上下文路径前缀（默认 /PythonWebServer）
CONTEXT_PATH=/PythonWebServer
# 日志级别（默认 INFO）
LOG_LEVEL=INFO
# 证书密钥密码
SERVER_KEY_PASSWORD=
